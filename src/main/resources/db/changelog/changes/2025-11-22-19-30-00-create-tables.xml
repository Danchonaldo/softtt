<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- 1) Create table countries -->
    <changeSet id="create-countries-table" author="admin">
        <createTable tableName="countries">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="varchar(255)">
                <constraints nullable="false"/>
            </column>
            <column name="code" type="varchar(10)">
                <constraints unique="true" nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <!-- 2) Create table items -->
    <changeSet id="create-items-table" author="admin">
        <createTable tableName="items">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="varchar(255)">
                <constraints nullable="false"/>
            </column>
            <column name="price" type="int">
                <constraints nullable="false"/>
            </column>
            <column name="quantity" type="int">
                <constraints nullable="false"/>
            </column>
            <column name="manufacturer_id" type="bigint"/>
        </createTable>
    </changeSet>

    <!-- 3) Add foreign key constraint -->
    <changeSet id="add-fk-items-manufacturer" author="admin">
        <addForeignKeyConstraint baseTableName="items"
                                 baseColumnNames="manufacturer_id"
                                 constraintName="fk_items_manufacturer"
                                 onDelete="CASCADE"
                                 onUpdate="RESTRICT"
                                 referencedTableName="countries"
                                 referencedColumnNames="id"
                                 validate="true"/>
    </changeSet>
</databaseChangeLog>